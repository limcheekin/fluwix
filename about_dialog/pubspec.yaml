name: about_dialog
description: About Dialog module.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  package_info_plus: ^4.0.2
  common_ui:
    path: ../common_ui
  showcase_view:
    path: ../showcase_view

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:

flutter:
  uses-material-design: true
  assets:
    - assets/logo_bright.png

  module:
    androidX: true
    androidPackage: com.example.about_dialog
    iosBundleIdentifier: com.example.aboutDialog
