name: percent_indicator_showcase
description: Percent Indicator Showcase module.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  percent_indicator: ^4.0.0
  responsive_builder:
  common_ui:
    path: ../common_ui
  showcase_view:
    path: ../showcase_view  

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  pedantic:

flutter:
  uses-material-design: true

  module:
    androidX: true
    androidPackage: com.example.percent_indicator_showcase
    iosBundleIdentifier: com.example.percentIndicatorShowcase
